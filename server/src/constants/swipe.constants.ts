/**
 * Swipe-related constants and enums
 */

export enum SwipeDirection {
  LEFT = "LEFT",   // Reject/Pass
  RIGHT = "RIGHT"  // Like/Accept
}

export const SWIPE_DIRECTIONS = Object.values(SwipeDirection);
